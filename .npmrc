# Use exact versions for dependencies
save-exact=false

# Engine strict mode (enforces Node.js version)
engine-strict=false

# Prefer exact versions in package-lock.json
package-lock=true

# Use legacy peer deps (useful for some packages)
legacy-peer-deps=false

# Progress bar
progress=true

# Log level
loglevel=warn
